{
  "name": "workspace-plugin-registry",
  "private": true,
  "type": "module",
  "description": "Index of community plugins for Workspace — links only, no plugin code",
  "scripts": {
    "validate": "node scripts/validate.js",
    "build-index": "node scripts/build-index.js"
  },
  "dependencies": {
    "sucrase": "^3.35.1"
  }
}
